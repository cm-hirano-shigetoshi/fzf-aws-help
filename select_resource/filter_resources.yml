base_task:
    input: bash $(dirname ${yml})/input.sh ${arg1} ${arg2}
    opts:
        - multi
        - reverse
        - preview-window='hidden'
expects:
    f5:
        continue:
            input: bash $(dirname ${yml})/input.sh ${arg1} ${arg2} --reload

